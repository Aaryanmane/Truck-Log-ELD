# Frontend Environment Variables Example
# In Vite, these are accessed via import.meta.env.VITE_*
# Create a .env.local file in the frontend folder with these values

# API Base URL - point to local backend during development
VITE_API_BASE_URL=http://localhost:8000

# Build mode (vite will set this automatically)
# VITE_ENV=development

